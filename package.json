{
  "name": "student-attendance-api",
  "version": "1.0.0",
  "description": "Student Attendance Mock API",
  "type": "commonjs",
  "scripts": {
    "server": "json-server --watch db.json --host 0.0.0.0 --port 4000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}